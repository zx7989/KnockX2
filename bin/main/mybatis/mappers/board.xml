<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
      PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.knockx2.board.dao.BoardDAO">
<resultMap id="articlesResult" type="articleVO">
    <result property="level" column="level" />
    <result property="articleNO" column="articleNO" />
    <result property="parentNO" column="parentNO" />
    <result property="title" column="title" />
    <result property="content" column="content" />
    <result property="memberSort" column="memberSort" />
    <result property="name" column="name" />
    <result property="writeDate" column="writeDate" />
    <result property="imageFileName" column="imageFileName" />
  </resultMap> 
  
  <resultMap id="imgResult" type="imageVO">
	  <result property="imageFileNO" column="imageFileNO" />
	  <result property="articleNO" column="articleNO" />
	  <result property="imageFileName" column="imageFileName" />
 </resultMap>

  <select id="selectAllArticlesList" resultMap="articlesResult"   >
    <![CDATA[
      SELECT level, articleNO, parentNO, title, content, writeDate, imageFileName, id,memberSort,name
      from p_board 
      START WITH  parentNO=0
      CONNECT BY PRIOR articleNO=parentNO 
      ORDER SIBLINGS BY articleNO DESC		
    ]]>
  </select>
  <select id="selectAllArticlesListPage" resultMap="articlesResult"   >
    <![CDATA[
    SELECT *
    FROM (
        SELECT level, articleNO, parentNO, title, content, writeDate, imageFileName, id, memberSort, name,rownum as rn
        FROM p_board where boardSort = 'free'
        START WITH parentNO = 0
        CONNECT BY PRIOR articleNO = parentNO
         ORDER SIBLINGS BY articleNO DESC	
    ) tmp
    WHERE rn >= #{offset} AND rn <= #{offset} + #{pageSize} - 1
    ]]>
  </select>
   <select id="selectAllInquiryList" resultMap="articlesResult"   >
    <![CDATA[
      SELECT level, articleNO, parentNO, title, content, writeDate, imageFileName, id,memberSort,name
      from p_board where boardSort = 'inquiry'
      START WITH  parentNO=0
      CONNECT BY PRIOR articleNO=parentNO 
      ORDER SIBLINGS BY articleNO DESC		
    ]]>
  </select>
  <select id="selectAllInquiryListPage" resultMap="articlesResult"   >
    <![CDATA[
    SELECT *
    FROM (
        SELECT level, articleNO, parentNO, title, content, writeDate, imageFileName, id, memberSort, name,rownum as rn
        FROM p_board where boardSort = 'inquiry'
        START WITH parentNO = 0
        CONNECT BY PRIOR articleNO = parentNO
         ORDER SIBLINGS BY articleNO DESC	
    ) tmp
    WHERE rn >= #{offset} AND rn <= #{offset} + #{pageSize} - 1
    ]]>
  </select>
   <select id="selectAdminArticlesList" resultMap="articlesResult"   >
    <![CDATA[
      SELECT level, articleNO, parentNO, title, content, writeDate, imageFileName, id,memberSort,name
      from p_board where boardSort = 'admin'
      START WITH  parentNO=0
      CONNECT BY PRIOR articleNO=parentNO 
      ORDER SIBLINGS BY articleNO DESC		
    ]]>
  </select>
 
	<!--단일이미지 추가  -->
	 
  <insert id="insertNewArticle"  parameterType="java.util.Map">
		<![CDATA[
			INSERT into p_board(articleNO,  title, content, imageFileName, id, memberSort, name,boardSort)
			VALUES(#{articleNO},#{title},	#{content}, #{imageFileName},#{id},#{memberSort},#{name},#{boardSort})
		]]>      
	</insert>
	
  <select id="selectNewArticleNO" resultType="int"  >
		<![CDATA[
			SELECT  nvl(max(articleNO), 0) + 1 from p_board		
		]]>
	</select>
	
  <select id="selectArticle" resultType="articleVO"   parameterType="int">
    <![CDATA[
      SELECT * from p_board
      where articleNO = #{articleNO}		
    ]]>
  </select>
  
  <update id="updateArticle"  parameterType="java.util.Map">
			update p_board
			set title=#{title},	 
			content=#{content}
			 <if test="imageFileName!='' and imageFileName!=null">
			  , imageFileName=#{imageFileName}
			</if>
			where articleNO=#{articleNO}
	</update>
	
	<delete id="deleteArticle"  parameterType="int">
    <![CDATA[
      delete from p_board
      where articleNO in (
         SELECT articleNO FROM  p_board
         START WITH articleNO = #{articleNO}
         CONNECT BY PRIOR  articleNO = parentNO )
    ]]>      
  </delete>
  
  <select id="selectShopArticlesList" resultMap="articlesResult"   >
    <![CDATA[
      SELECT level, articleNO, parentNO, title, content, writeDate, imageFileName, id,memberSort,name
      from p_board where boardSort = 'shop'
      START WITH  parentNO=0
      CONNECT BY PRIOR articleNO=parentNO 
      ORDER SIBLINGS BY articleNO DESC		
    ]]>
  </select>
  
  <select id="selectShopArticleListPage" resultMap="articlesResult"   >
    <![CDATA[
    SELECT *
    FROM (
        SELECT level, articleNO, parentNO, title, content, writeDate, imageFileName, id, memberSort, name,rownum as rn
        FROM p_board where boardSort = 'shop'
        START WITH parentNO = 0
        CONNECT BY PRIOR articleNO = parentNO
         ORDER SIBLINGS BY articleNO DESC	
    ) tmp
    WHERE rn >= #{offset} AND rn <= #{offset} + #{pageSize} - 1
    ]]>
  </select>

<select id="selectAppleArticlesList" resultMap="articlesResult"   >
    <![CDATA[
      SELECT level, articleNO, parentNO, title, content, writeDate, imageFileName, id,memberSort,name
      from p_board where boardSort = 'apple'
      START WITH  parentNO=0
      CONNECT BY PRIOR articleNO=parentNO 
      ORDER SIBLINGS BY articleNO DESC	
    ]]>
  </select>
  <select id="selectAppleArticlesListPage" resultMap="articlesResult"   >
    <![CDATA[
    SELECT *
    FROM (
        SELECT level, articleNO, parentNO, title, content, writeDate, imageFileName, id, memberSort, name,rownum as rn
        FROM p_board where boardSort = 'apple'
        START WITH parentNO = 0
        CONNECT BY PRIOR articleNO = parentNO
         ORDER SIBLINGS BY articleNO DESC	
    ) tmp
    WHERE rn >= #{offset} AND rn <= #{offset} + #{pageSize} - 1
    ]]>
  </select>
  
    <select id="selectSamsungArticlesList" resultMap="articlesResult"   >
    <![CDATA[
      SELECT level, articleNO, parentNO, title, content, writeDate, imageFileName, id,memberSort,name
      from p_board where boardSort = 'samsung'
      START WITH  parentNO=0
      CONNECT BY PRIOR articleNO=parentNO 
      ORDER SIBLINGS BY articleNO DESC	
    ]]>
  </select>
  <select id="selectSamsungArticlesListPage" resultMap="articlesResult"   >
    <![CDATA[
    SELECT *
    FROM (
        SELECT level, articleNO, parentNO, title, content, writeDate, imageFileName, id, memberSort, name,rownum as rn
        FROM p_board where boardSort = 'samsung'
        START WITH parentNO = 0
        CONNECT BY PRIOR articleNO = parentNO
         ORDER SIBLINGS BY articleNO DESC	
    ) tmp
    WHERE rn >= #{offset} AND rn <= #{offset} + #{pageSize} - 1
    ]]>
  </select>
 <!--다중 이미지 추가 SQL문 -->   
  <!--
    <insert id="insertNewArticle"  parameterType="java.util.Map">
    <![CDATA[
      insert into p_board(articleNO, id, title, content, imageFileName)
      values(#{articleNO}, #{id}, #{title}, #{content}, null)
    ]]>      
  </insert>

 
   <insert id="insertNewImage"  parameterType="java.util.Map">
    <foreach item="item"  collection="list"   open="INSERT ALL" 
                  separator=" " close="SELECT * FROM DUAL" >
      INTO t_imageFile(imageFileNO, imageFileName, articleNO, regDate)
      VALUES (#{item.imageFileNO}, #{item.imageFileName}, #{item.articleNO}, sysdate)
    </foreach>
  </insert>
 -->
   <!--
  <select id="selectNewImageFileNO" resultType="int"  >
    <![CDATA[
      SELECT  nvl(max(imageFileNO),0) from t_imageFile		
    ]]>
  </select>
	

  <select  id="selectImageFileList" resultMap="imgResult" parameterType="int">
    <![CDATA[
      SELECT * from t_imageFile
      where articleNO=#{articleNO}	
    ]]>
  </select> 
  -->
</mapper>